# linux-signed-* source packages are generated by the linux source
# package, and produce binaries with the same versions, even in case
# of a binNMU of linux
linux-signed@source_suffix@-@arch@ source: version-substvar-for-external-package Depends *${binary:Version} linux-headers-* -> linux-headers-*

# The changelog is mostly copied from linux which is non-native
linux-signed@source_suffix@-@arch@ source: odd-historical-debian-changelog-version * (for native)*
